@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(DSLtk)

include("${CMAKE_CURRENT_LIST_DIR}/EkippXTargets.cmake")

set(EkippX_INCLUDE_DIR "${PACKAGE_PREFIX_DIR}/@CMAKE_INSTALL_INCLUDEDIR@")
set(EkippX_INCLUDE_DIRS "${EkippX_INCLUDE_DIR}")

check_required_components(EkippX)
