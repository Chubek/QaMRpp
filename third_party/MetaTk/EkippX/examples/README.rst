EkippX Examples
===============

This directory contains small pass-1 examples aligned with the current runtime.

Each ``.eki`` file is intended to be expanded by ``ekippx-cli`` using either
``--eval`` or file input.
