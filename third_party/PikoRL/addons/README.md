# Addons

Installable user-facing packages belong here during source development.

- package assets and metadata together;
- install packages beneath `$PIKORL_HOME/addons`;
- do not place native shared objects here;
- do not treat embedded-runtime scripts as addon packages without packaging metadata.
